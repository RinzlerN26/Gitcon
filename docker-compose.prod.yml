version: '3.8'

services:
  spring-server:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    container_name: gitcon-spring-server
    ports:
      - "8080:8080"
    env_file:
      - .env